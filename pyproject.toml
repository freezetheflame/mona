[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mona"
version = "0.1.0"
description = "AI Agent Monitor — 事件驱动的智能命令监控器"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]
